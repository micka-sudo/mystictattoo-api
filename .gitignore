# === Node.js backend ===

# 📦 Dépendances
node_modules/

# 🔐 Variables d’environnement
.env
.env.local
.env.*.local

# ⚙️ Fichiers de configuration sensibles
config/admin.json

# 🪵 Fichiers de log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 🛠️ Fichiers IDE et systèmes
.vscode/
.idea/
.DS_Store

# 🧪 Dossiers de couverture de test et temp
coverage/
tests/temp/

# 📁 Build (si transpilation future ou React dans /client)
dist/
build/

# 📂 Dossier de fichiers uploadés (non versionnés)
uploads/

# 🔐 Scripts internes ignorés par défaut (sauf exception)
scripts/*
!scripts/generateSitemap.js


# ❌ Si tu avais un dossier à ignorer en erreur (corrigé ici)
# /middlewares/ ← à ne PAS ignorer, sinon le backend plante
