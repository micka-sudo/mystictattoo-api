services:
  - type: web
    name: mystictattoo-api
    env: node
    region: frankfurt
    branch: main
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: SECRET_KEY
        value: tonSuperTokenUltraSecret
      - key: ADMIN_PASSWORD
        value: admin123
      - key: PORT
        value: 4000
